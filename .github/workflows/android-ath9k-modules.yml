name: Build ath9k module (Android kernel)

on:
  workflow_dispatch:

jobs:
  build-ath9k:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            bc \
            bison \
            flex \
            libssl-dev \
            libelf-dev \
            dwarves \
            pahole \
            git \
            python3 \
            lz4 \
            liblz4-tool \
            wget \
            xz-utils \
            lld   # Gói này cài ld.lld từ hệ thống

      - name: Download clang r450784e
        run: |
          mkdir -p toolchain
          cd toolchain
          wget -q https://github.com/ravindu644/Android-Kernel-Tutorials/releases/download/toolchains/clang-r450784e.tar.gz
          tar -xf clang-r450784e.tar.gz

      - name: Download GNU aarch64 toolchain
        run: |
          cd toolchain
          wget -q https://github.com/ravindu644/Android-Kernel-Tutorials/releases/download/toolchains/arm-gnu-toolchain-14.2.rel1-x86_64-aarch64-none-linux-gnu.tar.xz
          tar -xf arm-gnu-toolchain-14.2.rel1-x86_64-aarch64-none-linux-gnu.tar.xz

      - name: Export toolchain environment & Check ld.lld
        run: |
          CLANG=$PWD/toolchain/clang-r450784e/bin
          GCC=$PWD/toolchain/arm-gnu-toolchain-14.2.rel1-x86_64-aarch64-none-linux-gnu/bin

          echo "ARCH=arm64" >> $GITHUB_ENV
          echo "LLVM=1" >> $GITHUB_ENV
          echo "LLVM_IAS=1" >> $GITHUB_ENV
          echo "CC=clang" >> $GITHUB_ENV
          echo "LD=ld.lld" >> $GITHUB_ENV
          echo "CROSS_COMPILE=aarch64-none-linux-gnu-" >> $GITHUB_ENV
          echo "CLANG_TRIPLE=aarch64-none-linux-gnu-" >> $GITHUB_ENV
          echo "PATH=$CLANG:$GCC:$PATH" >> $GITHUB_ENV
          echo "SKIP_ABI_CHECKS=1" >> $GITHUB_ENV
          echo "KMI_SYMBOL_LIST_STRICT_MODE=0" >> $GITHUB_ENV  # Thêm cái này nếu cần
          echo "TRIM_UNUSED_KSYMS=n" >> $GITHUB_ENV

          # Check ld.lld ngay sau khi PATH cập nhật (gộp debug)
          echo "=== Checking ld.lld ==="
          which ld.lld || echo "ERROR: ld.lld NOT FOUND in PATH!"
          ld.lld --version || echo "ERROR: ld.lld exists but cannot run!"
          clang --version
          echo "PATH is: $PATH"
          echo "===================="

      - name: Fix kernel script permissions
        run: |
          find kernel/scripts -type f -exec chmod +x {} \;

      - name: Prepare out directory
        run: |
          mkdir -p out

      - name: Load defconfig
        run: |
          make -C kernel O=out \
            ARCH=arm64 \
            CC=clang \
            LD=ld.lld \
            LLVM=1 \
            LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-none-linux-gnu- \
            SKIP_ABI_CHECKS=1 \
            IGNORE_ABI_CHECKS=1 \
            ENABLE_ABI=false \
            your_defconfig   # THAY BẰNG TÊN THỰC TẾ, ví dụ: gki_defconfig hoặc ath9k-specific_defconfig
            
      - name: Disable ABI whitelist check by emptying UNUSED_KSYMS_WHITELIST
        run: |
          # Chạy scripts/config để set UNUSED_KSYMS_WHITELIST thành rỗng
          ./kernel/scripts/config --file out/.config --set-str UNUSED_KSYMS_WHITELIST ""
          
          # Update lại config (để apply thay đổi, regenerate autoksyms nếu cần)
          make -C kernel O=out olddefconfig
          
          # Debug: kiểm tra config đã thay đổi chưa
          grep -E "TRIM_UNUSED_KSYMS|UNUSED_KSYMS_WHITELIST" out/.config || echo "No related config lines"
          
      - name: Disable TRIM_UNUSED_KSYMS to skip abi_symbollist check
        run: |
            sed -i 's/CONFIG_TRIM_UNUSED_KSYMS=y/# CONFIG_TRIM_UNUSED_KSYMS is not set/' out/.config || echo "CONFIG_TRIM_UNUSED_KSYMS not found, already disabled?"    
            make -C kernel O=out olddefconfig   
     
      - name: Prepare kernel for external modules
        run: |
          make -C kernel O=out \
            ARCH=arm64 \
            CC=clang \
            LD=ld.lld \
            LLVM=1 \
            LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-none-linux-gnu- \
            SKIP_ABI_CHECKS=1 \
            ENABLE_ABI=false \
            IGNORE_ABI_CHECKS=1 \
            SKIP_ABI_CHECKS=1 \
            ABI_DEFINES=1 \
            KBUILD_ABI=0 \
            prepare modules_prepare
            
      - name: Build ath9k module only
        run: |
          make -C kernel O=out \
            ARCH=arm64 \
            CC=clang \
            LD=ld.lld \
            LLVM=1 \
            LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-none-linux-gnu- \
            SKIP_ABI_CHECKS=1 \
            IGNORE_ABI_CHECKS=1 \
            KBUILD_ABI=0 \
            ENABLE_ABI=false \
            M=drivers/net/wireless/ath/ath9k \
            modules

      - name: Upload ath9k modules
        uses: actions/upload-artifact@v4
        with:
          name: ath9k-modules
          path: |
            out/drivers/net/wireless/ath/ath9k/*.ko
